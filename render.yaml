services:
  - type: web
    name: student-profile-analyzer
    runtime: python
    buildCommand: "pip install -r requirements.txt && python backend/create_db.py"
    startCommand: "gunicorn --chdir backend app:app"
